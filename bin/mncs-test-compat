#!/usr/bin/env bash
# Explicit migration/oracle adapter.  This path is never selected by the
# canonical launcher and must not be used for ordinary family verification.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/../tools/mncs_test.py" "$@"
